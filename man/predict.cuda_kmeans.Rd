% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algorithms.R
\name{predict.cuda_kmeans}
\alias{predict.cuda_kmeans}
\title{Assign observations with a fitted CUDA-aware k-means model}
\usage{
\method{predict}{cuda_kmeans}(
  object,
  newdata,
  type = c("cluster", "distance"),
  device = c("model", "auto", "cuda", "cpu"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{cuda_kmeans} object.}

\item{newdata}{A finite numeric matrix or data frame with observations in
rows and model features in columns. When omitted and \code{type = "cluster"},
the training assignments in \code{object$cluster} are returned.}

\item{type}{Return closest-centre \code{"cluster"} assignments or the
observation-by-centre \code{"distance"} matrix.}

\item{device}{Device used for the distance calculation. \code{"model"} reuses
the fitted model's actual distance device; \code{"auto"}, \code{"cuda"}, and
\code{"cpu"} follow the usual cudaverse device-selection rules.}

\item{...}{Must be empty.}
}
\value{
For \code{type = "cluster"}, an integer vector with observation names
and, for recomputed assignments, stage-level provenance. For
\code{type = "distance"}, a numeric matrix whose columns identify the fitted
centres. Omitting \code{newdata} returns validated stored training assignments
unchanged and does not create a prediction stage.
}
\description{
\code{predict.cuda_kmeans()} computes Euclidean distances to the fitted centres
and returns either the closest-centre assignment or the complete distance
matrix. Named features may be supplied in any order and are aligned safely.
}
\examples{
train <- as.matrix(iris[1:100, 1:4])
fit <- cuda_kmeans(train, centers = 3, seed = 1, device = "cpu")
predict(fit, as.matrix(iris[101:105, 1:4]), device = "cpu")
}
\seealso{
\code{\link[=cuda_kmeans]{cuda_kmeans()}}
}
