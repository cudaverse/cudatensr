% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{cuda_knn_graph}
\alias{cuda_knn_graph}
\title{Build a sparse graph from nearest neighbours}
\usage{
cuda_knn_graph(
  neighbors,
  weighting = c("binary", "distance", "gaussian"),
  symmetrize = c("union", "mutual"),
  sigma = NULL
)
}
\arguments{
\item{neighbors}{A \code{cuda_knn()} result or compatible list.}

\item{weighting}{Edge weighting: binary, inverse-distance, or Gaussian.}

\item{symmetrize}{Keep the union or only mutual nearest-neighbour edges.}

\item{sigma}{Gaussian bandwidth. Defaults to the median positive distance.}
}
\value{
A \code{cuda_graph} list containing sparse \code{adjacency}, counts of
\code{vertices} and undirected \code{edges}, \code{weighting}, \code{symmetrize},
\code{source_device}, and the graph-assembly \code{backend}. Named kNN observations
are retained as adjacency dimnames and in \code{vertex_names}.
}
\description{
The input may have been computed on CUDA, but graph assembly itself is
currently performed on the CPU with a sparse \code{Matrix}.
Union graphs retain an edge observed in either direction, whereas mutual
graphs require both directed neighbour relations. When the two directions
have different weights, the undirected edge retains the stronger affinity.
}
\examples{
index <- matrix(c(2, 3, 1, 3, 1, 2), 3, byrow = TRUE)
distance <- matrix(c(1, 2, 1, 1, 2, 1), 3, byrow = TRUE)
cuda_knn_graph(list(index = index, distance = distance))
}
