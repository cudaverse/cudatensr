% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor.R
\name{cudatensor-operators}
\alias{cudatensor-operators}
\alias{Ops.cudatensor}
\alias{\%*\%.cudatensor}
\title{Arithmetic operators for GPU-aware tensors}
\usage{
\method{Ops}{cudatensor}(e1, e2)

\method{\%*\%}{cudatensor}(x, y)
}
\arguments{
\item{e1, e2}{A \code{cudatensor} or numeric object for element-wise
arithmetic.}

\item{x, y}{A \code{cudatensor} or numeric matrix for matrix
multiplication.}
}
\value{
A \code{cudatensor} on the device of the tensor operand on the left (or
the tensor operand on the right when the left operand is a base object).
}
\description{
\code{cudatensor} objects support element-wise \code{+}, \code{-}, \code{*}, \code{/}, and \code{^}.
Operands follow trailing-dimension broadcasting. Mixed dtypes are promoted
without silently truncating fractional values; integer arithmetic is
promoted to \code{float64} to avoid R integer overflow.
Compatible dimension labels are retained. When both operands label the same
non-broadcast dimension, their labels must be identical.
}
\details{
Use \code{\%*\%} for matrix multiplication.
}
\examples{
x <- cuda_tensor(matrix(1:6, 2, 3), device = "cpu")
to_cpu(x + c(0.5, 1, 1.5))

y <- cuda_tensor(matrix(1:6, 3, 2), device = "cpu")
to_cpu(x \%*\% y)
}
