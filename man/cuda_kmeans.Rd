% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algorithms.R
\name{cuda_kmeans}
\alias{cuda_kmeans}
\title{GPU-aware k-means clustering}
\usage{
cuda_kmeans(
  x,
  centers,
  iter.max = 100L,
  tolerance = 1e-06,
  seed = NULL,
  device = c("auto", "cuda", "cpu")
)
}
\arguments{
\item{x}{Numeric matrix with observations in rows.}

\item{centers}{Number of clusters or a matrix of initial centres.}

\item{iter.max}{Maximum Lloyd iterations.}

\item{tolerance}{Convergence tolerance for centre movement.}

\item{seed}{Optional random seed used for initial centres.}

\item{device}{Device used for the distance step.}
}
\value{
A \code{cuda_kmeans} list containing integer \code{cluster} assignments,
final \code{centers}, per-cluster \code{withinss}, \code{tot.withinss}, the number of
\code{iter}ations, a logical \code{converged} flag, and the actual distance \code{device}.
Observation and feature names are retained when supplied.
}
\description{
GPU-aware k-means clustering
}
\examples{
set.seed(1)
x <- rbind(matrix(rnorm(40), 20, 2), matrix(rnorm(40, 4), 20, 2))
cuda_kmeans(x, centers = 2, seed = 1, device = "cpu")
}
