% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{cuda_stage}
\alias{cuda_stage}
\title{Record one compute stage}
\usage{
cuda_stage(
  requested_device,
  device,
  backend,
  selection_reason,
  fallback = FALSE,
  output_device = device
)
}
\arguments{
\item{requested_device}{\code{"auto"}, \code{"cpu"}, \code{"cuda"}, \code{"fixed-cpu"}, or
\code{"inherited"}.}

\item{device}{Actual compute device, \code{"cpu"} or \code{"cuda"}.}

\item{backend}{Concrete implementation backend.}

\item{selection_reason}{Stable reason describing device selection.}

\item{fallback}{Whether an \code{"auto"} request fell back to CPU.}

\item{output_device}{Device holding the returned value. Defaults to \code{device}.}
}
\value{
A validated \code{cuda_stage} list.
}
\description{
\code{cuda_stage()} is the shared constructor for cudaverse packages and
extensions. It distinguishes the requested device, actual compute device,
implementation backend, and device holding the returned value.
}
\examples{
cuda_stage(
  requested_device = "auto",
  device = "cpu",
  backend = "base",
  selection_reason = "cuda_unavailable",
  fallback = TRUE
)
}
